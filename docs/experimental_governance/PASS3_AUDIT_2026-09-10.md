# Governance Pass 3 Audit — Seal and Reproducibility

**Audit date:** 2026-09-10  
**Adopted contract:** `RAC_EXPERIMENTAL_GOVERNANCE_V1.md` + `IMPLEMENTATION_PASSES.md` Pass 3  
**Scope:** current implementation on the Friday-readiness branch, inherited from `main` at `eeeaa5cd3500aa6594db3b271e4a949a38d9bf6e`  
**Audit result:** **PASS — CLOSURE ELIGIBLE, subject to branch CI**  
**Evidence boundary:** synthetic governance fixtures only. This audit creates no physical evidence and does not modify any frozen D2 artifact.

## Per-criterion audit

| Adopted Pass 3 criterion | Evidence inspected | Result | Notes |
|---|---|---|---|
| `seal.py` validator/orchestrator | `ruthless_pipeline/governance/seal.py`; `tests/governance/test_pass3_adopted.py` | PASS | Governed cohort sealing and independent verification are implemented. |
| Sampling/cohort schema enforcement | governance sampling/cohort schemas + semantic validator; adopted test | PASS | Both manifests are validated against JSON Schema and semantic validation before sealing. |
| Referential integrity | registered constraint set, pipeline ID and calibration ID checks | PASS | Missing/unregistered references fail closed. |
| Artifact SHA-256 verification | artifact map verification tests | PASS | Content drift and artifact-set mismatch fail closed. |
| Source commit + software version binding | cohort manifest + seal identity tests | PASS | Code commit/version are bound into the seal material; version changes alter identity. |
| Seed binding | sampling manifest + absent-seed negative test | PASS | Missing seed prevents transition to `SEALED`. |
| Pipeline/calibration binding | governed seal arguments + reference-gate tests | PASS | Both references are required and independently checked. |
| Diagnostic-threshold hash | threshold bytes/hash binding + mutation test | PASS | A changed threshold after manifest construction invalidates the seal attempt. |
| Signed Git provenance where available | optional Git signature envelope | PASS | Optional provenance metadata is supported without changing scientific identity. |
| Optional trusted timestamp adapter | timestamp adapter fixture | PASS | Timestamp token can be added and independently verified without redefining the scientific seal. |
| Seal order / state transition | `PREFLIGHT -> SEALED` exit fixture + governance ledger | PASS | Seal is emitted only after validation gates; the state transition is ledgered. |
| CTM precondition: unsealed/incomplete cohort refused | `ruthless_pipeline/ctm/intake.py`; adopted tests | PASS | CTM requires the full governed verification context and rejects tampered input. |

## Required negative tests

All required negative cases in the adopted Pass 3 contract have direct coverage:

- absent seed → refused before `SEALED`;
- invalid referenced constraint set → refused;
- manifest count mismatch → refused;
- duplicate specimen ID → refused;
- artifact hash / artifact-set mismatch → refused;
- diagnostic-threshold drift → refused;
- unregistered pipeline/calibration reference → refused;
- forged/tampered seal → independent verification fails.

## Exit gate

The adopted exit gate requires a synthetic cohort to be sealed and then independently verified from complete inputs. `test_exit_gate_seals_then_independently_rederives_complete_cohort` performs that path, checks the governance ledger/state transition, and calls `verify_governed_seal` using the independently supplied sampling manifest, cohort manifest, artifacts, registered constraints/pipeline/calibration context, threshold bytes and diagnostic decision.

A separate deterministic-identity test proves repeated complete inputs produce the same scientific seal identity. Optional timestamp/Git envelopes are proven not to alter that identity.

**Exit-gate assessment: PASS.**

## Gaps / non-blocking observations

No adopted Pass 3 requirement is missing from the inspected implementation. The only remaining closure action is procedural: record a successful CI run for this branch/PR after the Friday-readiness changes and create/update the formal Pass 3 closure record against that verified commit.

This audit does **not** certify Passes 4–7 and does not change P1/P2/M1/M2 evidence state.
