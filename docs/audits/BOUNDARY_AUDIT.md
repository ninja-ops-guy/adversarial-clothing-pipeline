# Independent Boundary Audit — D2-0005 freeze candidate @ e222c67

**Overall: BOUNDARIES_HOLD_WITH_CAVEATS.** Auditor: independent agent (non-author of any audited artifact), verification-only, no edits, no pushes.

Repo: anonymous clone; HEAD = `e222c677106106d102df2ce8c3af1b33b45daa81` exactly (freeze candidate keyed to `67dd53f`; HEAD 5 commits newer — rehearsal commits only, verified via `git log`). Full suite: **579 passed** (matches rehearsal report claim). All 14 hash pins in `D2-0005_FREEZE_CANDIDATE.json` re-computed and match byte-for-byte (paired_arm `afce3df2…`, cluster module `bad02b22…`, prereg `5a26c083…`, memo `f05e2d05…`, proposal `aad8b812…`, design script `ddaab35f…`, OC results `5e8c78b1…`, runtime lock `3c7ca469…`, both model sets, all three freeze pins, amendment A5 `f2647f35…`).

## Boundary table

| # | Boundary | Verdict | Evidence |
|---|----------|---------|----------|
| 1 | HO-v3 isolation of surrogate selection | **PROVEN** | `make_config` (select_surrogate_candidate.py:39) hard-codes `heldout_models=()`; `build_evaluators(manifest, roles={"surrogate"})` (run_measured_benchmark.py:267) never instantiates HO weights; thresholds dict built only for `role=="surrogate"` (line 30-33). Live probe: injected a fake HO evaluator into the evaluator list — `ComparativeBenchmark.run` with `heldout_models=()` produced rows for surrogate only; HO evaluator never called. grep: no open of `model_sets/PERSON-HO-v3.json` anywhere in selection/freeze/benchmark import graph (only `finalize_model_lock.py` v1 bootstrap default and `build_d2_bundle.py` manifest copy — post-hoc identity copy, not scores). Rehearsal mocks keyed by HO model *names* only (rehearsal_d20005.py:107-110, 315). Pool guard refuses pools without `heldout_feedback_allowed: false` (line 203). |
| 2 | Freeze-before-access | **PROVEN (rehearsal state machine); STRUCTURAL-ONLY (real pipeline)** | Rehearsal: stage order `fixture→selection→freeze→analysis→seal→…` fixed in `STAGES` (line 122); `_journal_load` rejects any journal whose stage list isn't a prefix of `STAGES`. Probe: journal reordered to put `analysis` before `freeze` → `ValueError: journal stage order violates the rehearsal state machine` (refused). Freeze stage (lines 674-692) hashes telemetry before analysis stage (696-720) draws outcomes. Real pipeline: `run_measured_benchmark.py` runs surrogate+heldout in one process with no runtime gate on freeze; ordering is enforced only by artifact dependency (candidate-config.json must pre-exist) and by `freeze_adaptive_candidate.py` refusing reports with `heldout_models_loaded != []`. **No cryptographic interlock in the real benchmark script.** |
| 3 | Arm equivalence | **PROVEN with one disclosed asymmetry** | Same 100-candidate pool (seed 1337, pool-guard), same fixture/crops/sweep, same seeds, same invalid-condition filter (≤0.10) for both arms (select_surrogate_candidate.py:228, 248); `generations/RAC-PER-D2-0005.json` declares `differs_only_in: surrogate_optimization_objective`. Rehearsal mock arms share cluster set, 36-member grid, jitter, and draw keys (lines 310-323). **Caveat:** Arm M ranks by *pooled* `candidate_detection_rate` (sort_key, line 101) while Arm C ranks by CVaR of *per-surrogate* rates (objective_sort_key, line 110-114); if valid-condition counts differ across surrogate models, pooled rate ≠ mean of per-surrogate rates, so Arm M does not optimize exactly "mean per-surrogate rate". This is frozen D2-0004 behavior carried into the preregistration, not a new defect, but it is a treatment asymmetry in the strict sense. |
| 4 | Telemetry non-influence | **PROVEN** | Rehearsal `surrogate_selection_arm()` takes only `(pool_size, alpha)` — no telemetry input exists; bit-identical winner (`synthetic-cand-040`, CVaR 0.269727, sha 01428e7b… of full output) across two fresh processes. Real selector: `build_objective_telemetry` is write-only, opt-in, computed *after* winner selection (lines 252 vs 296-300); no read of telemetry/ledger/registry/generation in the selection path. Post-hoc telemetry alteration on a resumed run is refused via hash-mismatch (probe 5e). |
| 5 | Reruns fail closed | **PROVEN** | Independent injections (distinct from rehearsal's — crash between *analysis and seal*, then journal attacks): crash after `analysis` + intact journal → recovered run `summary_sha256` identical to clean run (`1e0daef8…`, matches report). Reordered journal → refused; garbled journal → `JSONDecodeError` (fails, no silent proceed); duplicated stage → `ValueError: refusing to double-emit`; tampered sealed artifact → `ValueError: …hash-mismatched on resume; failing closed`. Journal write is atomic (tmp+replace, line 592-596). **Caveat:** deleting the journal entirely + rerun re-computes and *silently overwrites* artifacts (probe: ran, hash identical). Determinism makes divergence impossible from identical inputs, but tamper-evidence is lost if the journal is destroyed — detection depends on journal survival. Real pipeline double-emit guard: `release directory already exists, refusing to overwrite` (ingest_closed_generation.py:430) + registry duplicate rejection. |
| 6 | Residual leakage (D2-0004 FAIL / −0.0987 / n=36 → K=72, seeds, regions) | **PROVEN (no channel found)** | grep: `0.0987`/`mean_delta` absent from all design scripts, OC artifacts, proposal, and amendment. All design inputs are hard-coded grid constants (`GRID_DELTAS=(-0.1,0,0.1,0.2,0.3)`, `GRID_ICCS=(0,.25,.5)`, `GRID_CLUSTER_COUNTS=(8,12,18,24,36,72)`, `SIM_SEED=20261209`; design_analysis_d20005_cluster.py:104-139). Critically, the Δ-grid including −0.1 and +0.2 predates the D2-0004 outcome: F0 study committed 2026-09-07 18:56 (`2ab3b4f`), D2-0004 closed 2026-09-08 05:17 (`c6fe509`). The post-closure v2 grid only *removed* 0.5 and added ICC/K axes from the same constants. K=72 is a disclosed grid pick (P(SUCCESS)≥0.8 at Δ=+0.2, ICC=0.25, gated by rehearsal). Decision regions, z, seeds, width gate, α, pool seed all unchanged from the frozen preregistration (verified identical constants in both statistics modules). The amendment is admittedly *motivated* by D2-0004's closure status — disclosed and legitimate pre-arming; no outcome *value* entered any parameter. |
| 7 | Guardrail integrity at HEAD | **PARTIALLY FALSIFIED as stated** | (a) `paired_arm_statistics.py` = `afce3df2…`; last touched 9cd2707 (2026-09-07, pre-closure). (b) `PREREGISTRATION_D2-0005.md` = `5a26c083…`; untouched since Wave E. (c) Core frozen surface untouched since 8851e01: `benchmark.py`, `evaluators.py`, `statistics.py`, `objectives.py`, `telemetry_contract.py`, `benchmarks/`, `generations/`, `model_sets/` — empty diff. **But** the literal claim "no active-generation edits since 8851e01" is false: `select_surrogate_candidate.py` (+2), `freeze_adaptive_candidate.py` (+9), `adapt_surrogate_shortlist.py`, `ingest_closed_generation.py`, etc. received additive `require_schema_version` fail-closed guards at 12:08 (`0efb0c9`), *after* closure (09:46). Verified the diffs are guard-only, no selection semantics changed. (d) "No manuscript/ writes since closure" is false as stated: 3 docs-audit commits (`e2596cf`, `64d675c`, `a326883`) edited `manuscript/backlog/paper{1,2,4,5}.md` post-closure — prose backlog only; `manuscript/exports|figures|evidence` untouched. |

## Violations / caveats (severity)

1. **Guardrail claims (7c, 7d) are literally inaccurate** — LOW severity: all post-closure edits are additive fail-closed schema guards or backlog prose; no frozen artifact, threshold, or result changed. Recommend restating the guardrail as "no *semantic* edits to active-generation logic; no manuscript *results/exports* writes."
2. **Freeze-before-access in the real pipeline is procedural, not enforced in `run_measured_benchmark.py`** — LOW/MEDIUM: the benchmark will happily run HO models on any candidate.png it is handed; ordering relies on artifact dependency + freeze-script assertions + journal (rehearsal only).
3. **Journal deletion defeats tamper-detection on rerun** (silent deterministic re-emit) — LOW: cannot change results without changing code/inputs, but the "tamper evidence" claim is journal-dependent.
4. **Arm M pooled-rate vs Arm C per-surrogate-rate ranking asymmetry** — LOW: preregistered, inherited from frozen D2-0004; matters only if valid-condition counts differ across surrogate models.

## Overall: BOUNDARIES_HOLD_WITH_CAVEATS

No evidence of held-out leakage into selection, no outcome-derived design parameters, freeze ordering and fail-closed rerun semantics verified by independent injection (all refused or byte-identical), every hash pin at HEAD verified. The four caveats above are documentation-precision and defense-in-depth issues, not boundary breaches.
