DEPRECATED — DO NOT USE AS AN INTEGRITY AUTHORITY
=================================================

This legacy top-level hash manifest is formally DEPRECATED as of the
Barrier 3 wave (2026-09-10). It has been stale since commit 1f42666
(Wave I item 7) and was never regenerated; 12 entries no longer match
their files. Keeping it as a live manifest would create two competing
hash authorities, which is prohibited.

Authoritative integrity systems (use these):

1. benchmarks/frozen_surface_sha256.json — the frozen scientific surface
   (generations/, protocols/, model_manifests/, model_sets/,
   design_profiles/, benchmarks/model_manifest.json,
   benchmarks/runtime_lock.json), guarded by
   tests/test_frozen_surface_integrity.py. 24/24 exact at deprecation time.
2. artifacts/barrier3/hashes.sha256 + run-manifest.json — Barrier 3
   integration package pinning, verified by
   ruthless_pipeline/integration/barrier3.verify_artifact_hashes.
3. artifacts/print-alpha/package-manifest.json — print package integrity
   (ruthless_pipeline/certification/print_package_integrity.py).

Historical entries are preserved in git history (see any commit at or
before f5fe07f). This file intentionally contains no live hash entries.
