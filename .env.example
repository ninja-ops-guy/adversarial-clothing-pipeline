# Logging: DEBUG, INFO, WARNING, ERROR
RUTHLESS_LOG_LEVEL=INFO

# Optional model caches. Keep credentials and weights out of git.
# HF_HOME=/path/to/huggingface/cache
# TORCH_HOME=/path/to/torch/cache
